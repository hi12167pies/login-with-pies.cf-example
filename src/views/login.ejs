<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
</head>
<body>
  <a href="<%= auth_url %>">Login with pies.cf</a>
  <% if (locals.text) { %>
    <br>
    <span><%= locals.text%></span>
  <% } %>
</body>
</html>